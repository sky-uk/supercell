machine:
  node:
    version: 6.1.0

general:
  artifacts:
    - "build/"
    - "package.json"

test:
  override:
    - npm run test-circle

experimental:
  notify:
    branches:
      only:
        - master
